//component to add a place on our app, most IMP because this will access all our native device features
import PlaceForm from '../components/Places/PlaceForm';
export default function AddPlace(){
    return <PlaceForm/>
};